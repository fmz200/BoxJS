# gitleaks 配置
# 以下路径均为已从当前代码树删除的遗留脚本目录（API key 残留于 git 历史中）。
# 待历史清理完成后可移除本 allowlist。新提交中的泄漏仍会被 CI 拦截。
[allowlist]
paths = [
  '''10000/''',
  '''10010/''',
  '''10086/''',
  '''apktw/''',
  '''midu/''',
  '''wanda/''',
]
